# [EBR2-96] Acceptance-suite pytest config.
[pytest]
addopts = -ra
markers =
    cli: REST/CLI acceptance tests (runs husky via the REST API, checks results)
    ui: browser-driven UI acceptance tests (Playwright, drives the real frontend)
# Browser defaults for pytest-playwright; override on the CLI with --headed etc.
